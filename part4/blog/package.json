{
  "name": "blog",
  "version": "1.0.0",
  "description": "fso blog",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development nodemon index.js",
    "test": "NODE_ENV=test node --test --test-concurrency=1",
    "build:ui": "rm -rf dist && cd ../../part5/blog && npm run build && cp -r dist ../../part4/blog",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "start:test": "NODE_ENV=test nodemon index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.1.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "supertest": "^7.2.2"
  }
}
